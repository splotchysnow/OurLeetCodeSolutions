#include <iostream>
#include <string>
#include <map>
#include <unordered_map>

using namespace std;

class Solution {
public:
    string longestCommonPrefix(vector<string>& strs) {
        
        string prefix = "";
        
        //Find the smallest length in the string array:
        

        
        
        
        
        
        //Take a character: set it as the first letter;
        char x = '\0';
        for (int i = 0; i < strs.size(); i++){
            if(i == 0){
                //On the first string:
                x = 1;
            }
            for(int j = 0; i < strs[i].length(); j++){
                
            }
        }
        
        return prefix;
    }
};